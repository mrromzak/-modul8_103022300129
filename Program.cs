




using mod_8;

class program
{

    public void main(String[] args)
    {
        BankTransferConfig config = new BankTransferConfig();

        if (config config lang = "en"){

            Console.WriteLine("please insert the amount of money to transfer");


        }

        else
        {
            Console.WriteLine("masukan jumlah uang yang akan di transfer");


        }

        int amount = Convert.ToInt32(Console.ReadLine());

        if (Config config transfer treshold == amount){ 
            
            int biayatranfer = config.config transfer.low_fee;

        }
        else 
        { 
            int biayatransfer = config.config transfer.high_fee;

        }
    }   
}